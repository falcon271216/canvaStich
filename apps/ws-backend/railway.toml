# Railway Root Directory: / (repo root)
# Railway reads this file if Config-as-code path = apps/ws-backend/railway.toml

[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/ws-backend/Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
