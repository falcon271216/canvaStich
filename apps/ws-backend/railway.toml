# Railway: Root Directory = repo root (/)
# Used when Root Directory is apps/ws-backend instead — see ../../railway.toml

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"

