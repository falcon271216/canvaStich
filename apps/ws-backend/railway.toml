# Railway → ws-backend service → Settings → Root Directory: apps/ws-backend
# Variables: DATABASE_URL, JWT_SECRET (from .env.ws-backend)

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "node dist/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
