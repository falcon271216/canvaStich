{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "cd ../.. && pnpm install --ignore-scripts && pnpm exec prisma generate --schema=packages/db/prisma/schema.prisma",
  "buildCommand": "cd ../.. && TURBO_TELEMETRY_DISABLED=1 pnpm turbo build --filter=http-backend...",
  "outputDirectory": "api",
  "rewrites": [{ "source": "/(.*)", "destination": "/api" }]
}
