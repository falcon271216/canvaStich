{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "cd ../.. && pnpm install --ignore-scripts && pnpm exec prisma generate --schema=packages/db/prisma/schema.prisma",
  "buildCommand": "pnpm run build",
  "outputDirectory": "api",
  "rewrites": [{ "source": "/(.*)", "destination": "/api" }]
}
