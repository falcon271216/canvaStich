# ws-backend on Railway — Root Directory must be "/" (repo root)
# Dockerfile path: apps/ws-backend/Dockerfile

[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/ws-backend/Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
